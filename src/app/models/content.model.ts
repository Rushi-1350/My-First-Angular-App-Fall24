// Defining the structure of content items for the portfolio
export interface IContent {
    title: string;
    description: string;
    technologies: string[];
    link: string;
    completed: boolean;
}